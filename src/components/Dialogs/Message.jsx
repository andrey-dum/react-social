import React from 'react';

//import './index.scss';

const Message = ({message}) => {
    return (
     
            <div className="message ">
                {message}
            </div>     
    
    );
}

export default Message;
